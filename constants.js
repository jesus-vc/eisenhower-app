export const PHONE_NUMBER_REGEX = /^[0-9]{10}$/;
//PEER In production code where the same regex pattern may be used in multiple JS and SQL schema files, are these regex patterns always hardcoded in SQL schema files? I ask since I imagine this could create room for inconsitency in the regex patterns.
